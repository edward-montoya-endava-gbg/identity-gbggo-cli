name: journey-device-start
description: Start a device session for a Captain v1 journey (customer token).
method: POST
path: /journey/device/start
auth: bearer
required_vars:
  - name: deviceType
    kind: string
    description: Device type identifier.
    example: "mobile"
body_template: |
  { "deviceType": {{ deviceType | json_encode }} }
