name: journey-state-delete
description: Delete state of a journey instance (destructive).
method: POST
path: /journey/state/delete
auth: bearer
required_vars:
  - name: instanceId
    kind: string
    description: Journey instance ID.
    example: "01HX..."
body_template: |
  { "instanceId": {{ instanceId | json_encode }} }
