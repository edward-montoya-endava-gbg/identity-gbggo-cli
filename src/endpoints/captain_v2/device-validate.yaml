name: device-validate
description: Validate a device session token.
method: POST
path: /device/validate
auth: bearer
required_vars:
  - name: token
    kind: string
    description: End-user JWT to validate.
    example: "eyJhbGciOi..."
body_template: |
  { "token": {{ token | json_encode }} }
