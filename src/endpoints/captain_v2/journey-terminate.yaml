name: journey-terminate
description: Terminate a running journey instance.
method: POST
path: /journey/terminate
auth: bearer
required_vars:
  - name: instanceId
    kind: string
    description: Journey instance ID.
    example: "01HX..."
body_template: |
  { "instanceId": {{ instanceId | json_encode }} }
